export enum TransactionType {
  DEBITO = 'débito',
  CREDITO = 'crédito',
}

export enum RecurrenceType {
  UNICA = 'única',
  PARCELADA = 'parcelada',
  RECORRENTE = 'recorrente',
}

export enum PeriodUnit {
  DIA = 'diária',
  SEMANA = 'semanal',
  MES = 'mensal',
  ANO = 'anual',
}
